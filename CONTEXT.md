# CONTEXT — MyTask-OS 术语表

本文件是领域术语表（glossary），只记录概念与边界，不含实现细节。

## 任务（Task）

六分区复选框行，`- [ ]` / `- [x]` 开头。身份 = 所在分区 + 行内文字（文件里无 ID）。

- **完成日期（doneDate）**：勾选后行尾的 ` ✅ YYYY-MM-DD`。只属于已勾选任务。
- **截止日期（dueDate）**：任务文字中的 ` 📅 YYYY-MM-DD`，属于任务本身，勾选与否无关。
  语义 =「这件事应当在此日期前完成」。与完成日期互不替代：一个管计划，一个管事实。

## 日历备注（Memo）

挂在某个具体日期上的**非任务**文字（纪念日、约会、提醒），无勾选框、无完成状态。
存储于独立的日历备注文件，一行一条：`- YYYY-MM-DD 备注文字`。
与任务的区别：任务会被勾选/拖拽/归档，备注只增删改，永不进入「已完成」。

## 分区（Zone）

六个固定分区之一：今日/临时/近期/长期/未安排/已完成。顺序固定，已完成区只进不出。

## 隔夜归档清扫（Sweep）

页面打开时把「已勾选且完成日期早于今天」的任务移入已完成顶部。
